apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-angular.fullname" . }}
  labels:
    {{- include "web-angular.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-angular.selectorLabels" . | nindent 4 }}
